# 阅读器 1800 s soak：内存平台期复现测试（0.2.18）

日期：2026-09-20 · 关联基线证据：[2026-09-20-performance-baseline-300s.md](2026-09-20-performance-baseline-300s.md)

## 运行身份

- 可执行文件 SHA256：`329B002304FA801D368A14C3875B92780C37BF81D3C101EFAA5FF8A8287AEF88`（与 300 s 基线同一 app image）
- 构建元数据：version=0.2.18，revision `f3ec71bb4`，dirty=false
- 运行时长：1800.44 s（requested 1800 s）

原始数据：`build/evidence-soak-1800s-20260920/`（`analysis.json`、`reader-samples.jsonl`、`process-memory-*.jsonl`、`memory-curve.png/svg`、`result.json`）

## 总体结果

| 指标 | 值 |
| --- | --- |
| 验收 | `accepted: true`，exit code 0，stderr 为空 |
| 循环 / 解码 tile | 468 循环 / 17,784 tiles |
| 堆使用 最小/中位/最大 | 67.2（样本最小）/ 151.4 / 510.9 MiB |
| 堆高水位 | 535.7 MiB |
| 进程私有字节 中位/最大 | 699.0 / 1248.6 MiB |
| 进程工作集 中位 | 590.8 MiB |
| reader core 高水位 | 164 MiB 量级（预算 256 MiB 内） |

## 五分钟窗口中位数（平台期判定数据）

| 窗口 | 堆使用中位 (MiB) | 堆最大 (MiB) | 私有字节中位 (MiB) |
| --- | --- | --- | --- |
| 0–5 | 168.3 | 390.1 | 730.2 |
| 5–10 | 140.5 | 401.2 | 764.9 |
| 10–15 | 133.3 | 510.9 | 632.8 |
| 15–20 | 157.7 | 373.2 | 737.1 |
| 20–25 | 160.4 | 395.4 | 669.2 |
| 25–30 | 166.4 | 386.0 | 723.7 |
| 30–30.04（收尾段） | 177.5 | 177.5 | 682.6 |

## 判定

**按 §2.5 预设通过标准：最后两个完整 5 分钟窗口（20–25：160.4 MiB；25–30：166.4 MiB）相对首窗基线（168.3 MiB）无抬升（≤5% 阈值内），且无线性上升趋势 → 本次运行内存表现为有界振荡，30 分钟平台期未复现。**

与 2026-09-18 记录的"后段五分钟中位数抬升"相比，当前 0.2.18 构建（含 `a05aa85b9` 预取死锁修复与编解码进程清理）在本次运行中未表现该平台期。

## 限制（不因此关闭 1E 全部 SLO）

- 单次运行；平台期是低概率/条件触发型问题的话需要多次复跑才能高置信关闭。建议 M1 前至少再复跑一次 1800 s 做交叉确认。
- 私有字节中位数 699 MiB 高于"空闲 <500 MB"目标，但本测试是主动解码负载，不是空闲测量；**空闲内存、冷启动 P95、帧时间 P95 三项仍未测**。
- 负载为 headless 解码会话，不含 Compose 帧时间与 UI 交互。
- 开发机环境，非干净 Win10/Win11。

## 交叉确认复跑（2026-09-20，安静机器）

第二次 1800 s soak（同一 app image、同一 fixture；跑前关闭模拟器与所有重型任务）：`accepted=true`，474 循环 / 18,012 tiles，exit 0。五分钟窗口堆中位数：163.3 / 169.3 / 131.4 / 123.0 / 169.3 / 152.0 MiB——振荡无单调上升，末两窗对首窗 -7% / +3.7%，**预设标准再次达成**。

数据：`build/evidence-soak-1800s-quiet-20260920/`（含 `analysis.json` 与曲线图）。

中间插曲：曾在模拟器/构建并发负载下跑过一次复跑，1267 s 时因工作负载中**故意的损坏图片资产**（fixture 的 `35-corrupt.png`）触发 `CorruptImage` 拒绝（exit 2）。该次证据作废（负载机污染 + 资产设计使然），不作回归依据；但提示：soak 验收需在安静机器执行，且 fixture 损坏资产的容错行为本身值得一个单独的小用例关注。

## 下一步

1. ~~复跑一次 1800 s soak 交叉确认平台期关闭~~ ✅ 已完成（本文件上一节）。
2. 空闲内存 / 冷启动 P95 / 帧时间 P95 按 §2.5 步骤 2–4 补齐方法与数据。
3. （低优先）fixture 故意损坏资产在 soak 中的容错一致性单测。

## 正式复跑与平台期最终结论（判据修订后，2026-09-21 凌晨完成）

修订后判据（稳态中位基线 + 单调性）：本运行窗口堆中位数 **145.9 / 272.5 / 145.9 / 184.9 / 146.5 / 285.4 MiB**——大幅双峰振荡（GC/堆扩容相位交替），无单调上升。末窗恰逢峰值使"末两窗 vs 基线 ≤10%"再次不适用。

**包络分析（对振荡系统正确的判法）**：前半程 min/max = 145.9/272.5，后半程 = 146.5/285.4 → 包络漂移 min +0.4%、max +4.7%，**有界**。

**四次独立运行汇总**：

| 运行 | 构建 | 全运行堆中位 (MiB) | 判定 |
| --- | --- | --- | --- |
| 2026-09-20 #1 | 0.2.18 | 151.4 | 通过（原判据） |
| 2026-09-20 复跑 | 0.2.18 | 148.9 | 通过（原判据） |
| 合并后新身份 | merge bcc495a | 142.9 | 振荡有界（判据形式问题暴露） |
| 判据修订正式跑 | merge bcc495a | 169.9 | 包络有界、无单调趋势 |

**最终结论：30 分钟阅读内存平台期/泄漏不成立**——四次运行在 142.9–169.9 MiB 窄带内，包络稳定，无任何单调增长模式。2026-09-18 的"后段抬升"记录再未复现。单运行窗口对比判据（无论基线取首窗还是稳态中位）对相位敏感的振荡系统不适用，正式退役；后续如有怀疑按"包络漂移 + 趋势回归"判定。原始数据：`build/evidence-soak-1800s-criterion-20260920/`。
