# 证据日志：Workstream 1D 干净虚拟机环境搭建 (VMware Workstation)

| 元信息 | 值 |
| --- | --- |
| 日期 | 2026-09-20 |
| 任务 | Workstream 1D 干净机器发行验收环境准备 (Windows 11 x64) |
| 宿主机环境 | Windows 11 Host, VMware Workstation 17.x |
| vmrun 路径 | `C:\Program Files (x86)\VMware\VMware Workstation\vmrun.exe` |
| vdiskmanager 路径 | `C:\Program Files (x86)\VMware\VMware Workstation\vmware-vdiskmanager.exe` |
| 虚拟机工作目录 | `H:\mihondesk-vm\win11-test\` |
| 状态 | 进行中 (Phase 1 与 Phase 2 均已完成；进入 Phase 3 安装与基线快照) |

---

## 1. 宿主机与工具链准备

### 1.1 磁盘空间与路径
- 宿主机 H 盘可用空间: ~279 GB
- 宿主机 E 盘可用空间: ~311 GB
- 虚拟机工作目录: `H:\mihondesk-vm\`

### 1.2 VMware 工具链核查
- `vmrun.exe`: 存在于 `C:\Program Files (x86)\VMware\VMware Workstation\vmrun.exe` (版本 2024-11-28 构建)
- `vmware-vdiskmanager.exe`: 存在于 `C:\Program Files (x86)\VMware\VMware Workstation\vmware-vdiskmanager.exe`
- ADK 工具 (`oscdimg.exe`): 未在系统默认路径检出，采用标准 FAT12 1.44MB `.flp` 软盘镜像承载 `autounattend.xml` 自动应答文件（VMware 原生支持挂载 `.flp`，Windows 安装程序启动时自动读取软盘根目录应答文件）。

### 1.3 现有发行包资产核查
- `desktop-app/build/releases/` 目录中已存在 0.2.2 至 0.2.10 以及 0.2.18 等历史构建产物（包含 EXE 安装包、MSI 安装包、便携 ZIP、SHA256SUMS.txt）。

---

## 2. Phase 1 — 官方 Windows 11 Evaluation ISO 获取

### 2.1 端点探测与选型
- 微软 Evaluation Center 官方静态分发源：
  `https://software-static.download.prss.microsoft.com/dbazure/888969d5-f34g-4e03-ac9d-1f9786c66749/26100.1742.240906-0331.ge_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_zh-cn.iso`
- 版本：Windows 11 24H2 Enterprise Evaluation (Build 26100.1742) x64 简体中文版
- 响应头验证：`HTTP/1.1 200 OK`，`Content-Length: 5602680832` 字节 (~5.22 GB，符合 >4GB 完整镜像要求)。
- 下载命令：`curl.exe -L --fail --retry 10 --retry-delay 3 -o "H:\mihondesk-vm\26100.1742.240906-0331.ge_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_zh-cn.iso" "https://software-static.download.prss.microsoft.com/dbazure/888969d5-f34g-4e03-ac9d-1f9786c66749/26100.1742.240906-0331.ge_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_zh-cn.iso"`
- 状态: **已完成 (Phase 1 Complete)**。
- 实际落地大小: `5602680832` 字节 (精准匹配，>4GB)。
- SHA256 校验和: `16a0fd95f0d84e54e260d9f129b39e3d3160d7b294d986ddbb3a99b3828d1c62` (Python hashlib 与 PowerShell Get-FileHash 双重交叉比对完全一致)。
- 7-Zip 完整性校验 (`7z t`):
  ```
  Path = H:\mihondesk-vm\26100.1742.240906-0331.ge_release_svc_refresh_CLIENTENTERPRISEEVAL_OEMRET_x64FRE_zh-cn.iso
  Type = Udf (Version 1.02, Cluster Size = 2048, Sector Size = 2048)
  VolumeId = UDF Volume, VolumeSetId = e1cf4000MS UDFBridge
  Logical Volumes = CENA_X64FREE_ZH-CN_DV9
  Folders: 102, Files: 1057, Size: 5596748091, Compressed: 5602680832
  Everything is Ok
  ```
  镜像文件系统与数据校验 100% 通过。


---

## 3. Phase 2 — 无人值守安装自动化与虚拟机配置

### 3.1 虚拟磁盘创建
- 命令: `vmware-vdiskmanager.exe -c -s 80GB -a lsilogic -t 0 "H:\mihondesk-vm\win11-test\win11-test.vmdk"`
- 结果: 成功创建 80GB 单文件动态扩容虚拟磁盘 (`win11-test.vmdk`)。

### 3.2 自动应答文件 `autounattend.xml`
- 文件路径: `H:\mihondesk-vm\win11-test\autounattend.xml`
- 关键配置：
  - `windowsPE` 阶段：
    - 自动 GPT 分区：300MB EFI 系统分区 (FAT32)、128MB MSR 分区、剩余空间为主分区 (C: 盘 NTFS)
    - 自动安装到 Disk 0 Partition 3，镜像索引：`/IMAGE/INDEX = 1`（匹配评估版单一镜像）
    - 自动接受 EULA 许可协议
    - `RunSynchronousCommand` 注入 `LabConfig` 虚拟化硬件免检项：`BypassTPMCheck=1`, `BypassSecureBootCheck=1`, `BypassRAMCheck=1`, `BypassCPUCheck=1`, `BypassStorageCheck=1`
  - `specialize` 阶段：
    - 计算机名：`win11-test`
    - 时区：`China Standard Time`
  - `oobeSystem` 阶段：
    - 区域语言：`zh-CN`
    - OOBE 页面全屏蔽：隐私设置拒绝 (`ProtectYourPC=3`)，跳过联机微软账户屏幕、EULA 屏幕、网络设置屏幕
    - 自动创建本地管理员账户：用户名 `testuser`，密码 `Test1234!`
    - 开启 AutoLogon (`testuser` 自动登录，次数 999)
    - `FirstLogonCommands` 自动配置 PowerShell 执行策略 `RemoteSigned`

### 3.3 软盘镜像生成与验证 (`autounattend.flp`)
- 生成工具: 自主编写纯 Python FAT12 格式化与写入工具 (`scratch/make_flp.py`)，精准生成 1,474,560 字节 (2880 扇区 × 512 字节) 标准 1.44MB 软盘镜像。
- 根目录写入:
  - 长文件名 (VFAT LFN): `autounattend.xml` (校验和精准关联)
  - 短文件名 (8.3 fallback): `AUTOUNAT.XML`
- 7-Zip 校验结果:
  ```
  Type = FAT
  File System = FAT12
  Sector Size = 512
  2026-09-20 12:00:00 ....A 7193 autounattend.xml
  2026-09-20 12:00:00 ....A 7193 AUTOUNAT.XML
  ```
  校验 100% 通过。

### 3.4 虚拟机配置 `win11-test.vmx`
- 路径: `H:\mihondesk-vm\win11-test\win11-test.vmx`
- 硬件规格:
  - 4 vCPU (`numvcpus = 4`, `cpuid.coresPerSocket = 2`)
  - 8GB RAM (`memsize = 8192`)
  - 固件: EFI (`firmware = "efi"`, `uefi.secureBoot.enabled = "TRUE"`)
  - SCSI 控制器: `scsi0.virtualDev = "lsisas1068"`, 挂载 `win11-test.vmdk`
  - CD-ROM: `ide1:0`, 挂载官方 Windows 11 Enterprise Eval ISO
  - 软驱: `floppy0`, 挂载 `H:\mihondesk-vm\win11-test\autounattend.flp`
  - 网络: NAT (`ethernet0.virtualDev = "e1000e"`)
  - 自动响应: `msg.autoAnswer = "TRUE"` (抑制宿主机交互弹窗)

### 3.5 VMware Tools 离线包与控制器准备
- 宿主机预下载官方静默安装包: `H:\mihondesk-vm\VMware-tools-x64.exe` (版本 13.1.5-25544008，大小 140,467,592 字节)，作为虚拟机内部自动联网下载失败时的本地后备。
- 自动化控制脚本: `H:\mihondesk-vm\capture.py` 与 `monitor_loop.py`，基于 RFB 3.8 协议直连 VNC 控制台实现无窗口截屏与状态跟踪。

---

## 4. Phase 3 — 无人值守安装、系统就绪与基线快照

### 4.1 虚拟机启动与引导
- 启动命令: `vmrun.exe -T ws start "H:\mihondesk-vm\win11-test\win11-test.vmx" nogui`
- 引导自动化: 虚拟机通过 EFI 固件加载 Windows 11 安装介质。为绕过 EFI CD-ROM 的 "Press any key to boot from CD or DVD" 倒计时提示，自动化控制器在启动后通过 VNC RFB 协议向控制台注入 Enter 键事件，成功触发安装程序加载。
- 硬件兼容性与绕过: `autounattend.xml` 中配置的 `LabConfig` 自动注册表脚本执行生效，完全豁免 TPM 2.0、SecureBoot、内存与 CPU 硬件检测。
- 磁盘初始化: Windows Setup PE 自动识别 80GB 单磁盘 (Disk 0)，自动创建 EFI (300MB)、MSR (128MB)、NTFS 主分区 (C: 盘)，并选定 `/IMAGE/INDEX = 1` 自动开始应用映像。

### 4.2 安装进度跟踪 (实时状态)
- 00:20:49: 镜像写入 ~18% (磁盘 ~2.1 GB)
- 00:23:30: 镜像写入 ~27% (磁盘 ~3.4 GB)
- 00:24:35: 镜像写入 ~40% (磁盘 ~5.1 GB)
- 00:26:24: 镜像写入 ~52% (磁盘 ~7.4 GB)
- 00:27:22: 镜像写入 ~57% (磁盘 ~8.1 GB)
- 安装过程平稳展开，无任何人工介入弹窗阻滞。


---

## 4. Phase 3 完成记录（agy 配额中断后由主代理接管完成）

### 4.1 Windows 安装结果
- 无人值守安装**完全成功**：`autounattend.xml`（FAT12 软盘承载）全自动完成分区/EULA/OOBE，自动登录 `testuser`。
- 版本实证（控制台截图水印）：**Windows 11 企业评估版 Build 26100.ge_release.240331-1435**（zh-cn）。

### 4.2 VMware Tools 安装的探索与暂缓（记录在案）
- agy 打包的 `vmtools.iso` 只含 setup.exe，静默安装瞬时失败（缺配套文件）；提权通道本身可疑（Start-Process -Verb RunAs 无可见 UAC、瞬时返回）。
- 尝试路径：官方 packages.vmware.com ISO（Broadcom 迁移后 404）、msiexec 管理安装（exe 为引导程序非 MSI）、7z 流提取（得到 CAB 但重组成本高）。
- **结论：矩阵执行不依赖 Tools**——工件经 IMAPI2 构建 ISO 挂载，命令经 VNC 键入（已验证可用），快照经 vmrun。Tools 安装记为后续便利项。

### 4.3 干净基线快照
- `vmrun snapshot win11-test clean-baseline`：**Total snapshots: 1**（win11-test-Snapshot1.vmsn）。
- 快照时 VM 处于已安装未装任何第三方软件状态（仅挂载过工具盘，未安装成功，无残留）。

## 5. 下一阶段（矩阵执行）就绪清单
- [x] 干净 VM + 基线快照
- [x] VNC 控制台控制链（截图/键入脚本，H:/mihondesk-vm/*.py）
- [x] 发行工件在场：`desktop-app/build/releases/0.2.18/`（EXE/MSI/便携 ZIP/SHA256SUMS，含 0.2.17 可用作 N-1）
- [ ] IMAPI2 工件 ISO 构建脚本（矩阵前编写）
- [ ] 12+2 格执行（每格：装前快照 → 执行 → 取证 → 回滚基线）
